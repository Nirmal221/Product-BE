# Server Configuration
PORT=3000
NODE_ENV=development

# MongoDB Configuration
# Option 1: Full connection string (recommended)
# Structure: ShoesDB -> [database_name] -> [collection_name]
# For local MongoDB:
MONGODB_URI=mongodb://localhost:27017/shoes

# For MongoDB Atlas (Cluster: ShoesDb):
# MONGODB_URI=mongodb+srv://username:password@ShoesDb.mongodb.net/shoes?retryWrites=true&w=majority

# Option 2: Separate base URI and database name
# MONGODB_BASE_URI=mongodb+srv://username:password@ShoesDb.mongodb.net
# DB_NAME=shoes

# To use a different database (e.g., "Hello"), just change the database name:
# MONGODB_URI=mongodb+srv://username:password@ShoesDb.mongodb.net/Hello?retryWrites=true&w=majority
# OR
# DB_NAME=Hello

# JWT Secret (if you plan to add authentication)
JWT_SECRET=your-secret-key-here
